# MEA Editor - Python dependencies
# Install: pip install -r requirements.txt

# Qt GUI framework
PySide6>=6.5.0

# Numerical computing (used by electrode_matrix_io and probeinterface)
numpy>=1.24.0

# Read/write probeinterface JSON files
probeinterface>=0.3.0

# Build standalone executable (build_exe.py)
PyInstaller>=6.0.0
